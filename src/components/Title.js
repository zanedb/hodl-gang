import React from 'react'
import { Section, Heading } from '@hackclub/design-system'
import '../App.css'

const Title = () => (
  <Section bg="accent">
    <Heading f={7}>
      #HODLGANG
    </Heading>
  </Section>
)

export default Title
